import styles from './MiningPanel.module.css'

export const MiningPanel = () => {
      return (
            <div className={styles.panel}>
                  <div className={styles.pageTitle}>Mining</div>
            </div>
      );
}